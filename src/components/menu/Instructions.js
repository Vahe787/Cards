import React from "react";

const Instructions = () => {
  return (
    <div className="border-l-2 border-gray-100">
      <p className="mt-44 p-3 font-bold w-62 bg-clip-text text-transparent bg-gradient-to-r from-pink-500 to-violet-500">
        Press the "Add Card" button to add the new Card. use the "Sort Cards"
        button to sort the Cards by the increase. Press an delete icon on the
        top right to delete item
      </p>
    </div>
  );
};

export default Instructions;
